// regex: ID=[[:alpha:]0-9]+

script {
    entry fn main() -> () {
        local b256 dada = const b256 0xdadadadadadadadadadadadadadadadadadadadadadadadadadadadadadadada

        block0():
        v0 = get_local ptr b256, dada
// check: $(var=$ID) = get_local ptr b256, dada
        v1 = load v0
        v2 = const u64 22
        v3 = call foo(v1, v2)
// check: call foo($var, v2)
        v4 = const unit ()
        ret () v4
    }

    fn foo(x: b256, y: u64) -> () {
// check: fn foo(x: ptr b256, y: u64) -> () {
        entry(x: b256, y: u64):
// check: entry(x: ptr b256, y: u64):
        v0 = const u64 0
        log u64 y, v0
        v1 = const unit ()
        ret () v1
    }
}
